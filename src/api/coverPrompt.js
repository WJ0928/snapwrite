export const getCoverPrompt = (content) => `
你是一名资深视觉设计师 + 前端工程师，请根据文章内容生成一个“微信公众号封面 HTML（用于 html2canvas 截图导出）”。

【输入内容】
{{ ${content} }}

【目标】
生成一个高点击率、信息清晰、视觉有冲击力的公众号封面（横版）

【HTML 输出要求（必须严格遵守）】

1. 只输出 HTML 字符串，不要解释、不要注释、不要 markdown
2. 不要包含 <html> <head> <body> 标签，只输出一个根 div
3. 所有样式必须使用 inline style（禁止外部 CSS）
4. 不要使用任何外部资源（字体、图片、icon CDN 等）
5. 尺寸必须固定为：
   - width: 900px
   - height: 383px
6. 使用系统字体：
   - font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto
7. 布局必须稳定：
   - 使用 flex / absolute
   - 避免复杂布局（grid / 依赖计算的布局）
8. 所有内容必须在可视区域内（避免裁剪）
9. 颜色必须使用 HEX（如 #0f172a）
10. 文本必须精简，避免换行混乱
11. 关键内容必须放在安全区域（左右各预留 40px padding）

【内容提炼要求】

1. 自动提炼：
   - 主标题（<= 10字，必须有冲击力）
   - 副标题（<= 20字）

2. 标题必须具备至少一个：
   - 情绪词（如：崩溃 / 爆了 / 离谱）
   - 数字（如：3个 / 10倍）
   - 结果导向（如：直接起飞 / 提升300%）

【视觉设计要求】

1. 风格：
   - 高对比 / 强视觉冲击 / 现代感（科技 / 商业 / 情绪化）

2. 配色：
   - 使用 1个主背景色（或渐变）
   - 1个强调色（用于标题或标签）
   - 1个辅助色
   - 保证对比明显
   - 文字 、背景、强调色、辅助色 都要有，对比明显

3. 排版（必须实现其一）：
   - 左侧文案 + 右侧视觉元素（推荐）
   或
   - 居中大标题（极简风）

4. 字号建议：
   - 主标题：40px、56px，加粗
   - 副标题：20px、28px
   - 标签：16px、20px

5. 视觉增强：
   - 标题可使用 text-shadow
   - 标签必须使用高亮块（背景色 + padding + 圆角）
   - 可使用简单几何图形增强层次（圆 / 渐变块）

【SVG 增强要求（必须使用）】

1. 至少使用 1~2 个 inline SVG 元素
2. SVG 必须直接写在 HTML 中（禁止外链 / img）
3. SVG 用于：
   - 背景装饰（线条 / 网格 / 渐变光效）
   - 或右侧视觉焦点（圆形 / 抽象图形）

4. SVG 约束：
   - 使用基础元素：line / circle / rect / polyline / path
   - 可以使用 linearGradient / radialGradient
   - 禁止复杂 filter（避免 html2canvas 渲染问题）
   - 颜色必须与整体配色一致
   - 尺寸必须可控，不得溢出容器

【布局细节要求】

- 整体使用 flex 布局
- 文案区域宽度不超过 60%
- 视觉元素放右侧或背景层（position:absolute）
- 层级清晰（必要时使用 z-index）
- 保证视觉中心明确（有一个“焦点”）

【输出格式】

直接输出 HTML，例如：

<div style="...">
  ...
</div>
`;
